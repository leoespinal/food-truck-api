import { Router } from "express";

// export default({ config, db => {
//   let api = Router();
//
//   // add middleware
//
//   return api;
// });
export default(function(config, db) {
  let api = Router();

  // add middleware

  return api;
});
